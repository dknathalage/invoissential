{
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "deploy": "turbo run deploy"
  },
  "devDependencies": {
    "prettier": "^3.2.5",
    "prettier-plugin-svelte": "^3.2.2",
    "turbo": "^2.3.3"
  },
  "engines": {
    "node": ">=18"
  },
  "name": "with-svelte",
  "packageManager": "npm@10.7.0",
  "workspaces": [
    "modules/*"
  ]
}
